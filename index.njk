---
title: A eleventy taster page
layout: base.njk
eleventyImport:
  collections: ["post"]
numberOfLatestPostsToShow: 2  
pagination: 
  data: collections.posts
  size: 4
  reverse: true
---
{# Hero section #}

 <div class="container about-section">
  <div class="row align-items-center">
    <div class="col-12 col-md-6 mb-5 hero-title" > 
      <h1 class="mb-4">JAMstack Blog: <br> Tips, Tricks, and Insights</h1>
      <p class="pe-5">Discover articles covering web development using JAMstack principles</p>
    </div>
      <div class="col-12 col-md-6">
        <img  class="img-fluid rounded" src="../assets/images/ai-programmer-desk photo.jpg" alt="Ai generated image of a isometric 3D office full of monitors with blue screens, a orange desk and cup of coffee">
      </div>
  </div>
 </div>




{# Latest post hero section #}
{# mostrecent contain the data on the latest post aka last post to be posted, start would be the oldest post #}
{% set mostRecent = collections.posts | last %}

<div class="p-4 p-md-5 mb-4 mt-4 text-white rounded image-fluid overlay-image" style="  background:  linear-gradient(90deg, rgba(26,32,44,1) 0%, rgba(65,65,65,0.5328198173800771) 100%), url('{{mostRecent.data.image}}');  background-size: cover;" >
  <div class="col-md-6 px-0">
    <h1 class="display-4 fst-italic">{{mostRecent.data.title}}</h1>
    <p class="lead my-3">{{ mostRecent.data.description }}</p>
    <p class="lead mb-0"> 
      <a href="{{ mostRecent.url }}" class="text-white fw-bold">Continue reading...</a>
    </p>
  </div>
</div> {# Latest post hero section end here #}

{# Featured post list cards #}
<div class="container p-md-0 post-grid">
<div class="row mb-2">
  {% for post in pagination.items %}
  {% if loop.first %}
  {# Use loop.first to check for first element in array and skip it as it contains the hero section post #}
  {% else %}
  <div class="col-md-4 d-flex align-items-stretch"> {# flex and align to make card same height #}
    <div class="card border-dark mb-3">
      <img src="../assets/images/abstract-blurred-multi-colored-background.jpg" class="card-img-top" alt="...">
        <div class="card-body d-flex flex-column">
      <h5 class="card-title">{{ post.data.title }}</h5>
      <p class="card-text">{{ post.data.description }}</p>
      {# Add flex and direction to parent, and mt-auto to element to make it stick to bottom of card #}
      <p class="card-text mt-auto"><small class="text-body-secondary">Updated {{ post.date | postDate}}</small></p>
        </div> 
    </div> 
  </div>
  {% endif %}
  {% endfor %} 
</div>

{# Main blog content #}




{#     <div class="container">
        <h1>Need help with eleventy? Call on discord</h1>
        <div class="content">Going great so far, installed setup the file structure and installed eleventy</div>
        <p>Now trying to add a new index template</p>
    
        <div class="container"> 
            <h1>Testing json - Tech News</h1>
            <ul>
                {% for story in news %}
                    <h2>{{ story.title }}</h2>
                    <p><a href="{{story.url}}" target="_blank" rel="noopener noreferrer">{{ story.url}}</a></p>
                {% endfor %}
            </ul>
      {{news | dump}}  use to dump full json for troubleshot 
        
        </div>
    </div> #}